import { PerfilDirective } from './perfil.directive';

describe('PerfilDirective', () => {
  it('should create an instance', () => {
    const directive = new PerfilDirective();
    expect(directive).toBeTruthy();
  });
});
